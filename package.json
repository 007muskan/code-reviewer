{
  "name": "code-review",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "npm install --legacy-peer-deps --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "npm install --prefix backend && npm run start --prefix backend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
